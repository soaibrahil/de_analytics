"""Bronze layer package

Provides simple helpers to move data from a landing (ingest) zone
into the bronze storage area (raw persisted data). This package
contains small, dependency-free utilities intended as a starting
point for ingestion pipelines.
"""